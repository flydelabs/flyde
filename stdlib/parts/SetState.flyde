imports: {}
exports:
  - SetState
parts:
  SetState:
    id: SetState
    inputs:
      key:
        mode: required
        type: any
      value:
        mode: required
        type: any
    outputs:
      r:
        type: any
    fnCode: |+
      const {state} = adv;
      const {key, value} = inputs;

      state.set(key, value);
      outputs.r.next(value);

exposedFunctionality: []
