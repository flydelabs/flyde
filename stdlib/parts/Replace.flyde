imports: {}
exports:
  - Replace
parts:
  Replace:
    id: Replace
    inputs:
      str:
        mode: required
        type: any
      from:
        mode: required
        type: any
      to:
        mode: required
        type: any
    outputs:
      r:
        type: any
    completionOutputs:
      - r
    fnCode: |-
      const { str, from, to } = inputs;
      const { r } = outputs;
              
      // magic here
      r.next(str.replace(from, to));
exposedFunctionality: []
