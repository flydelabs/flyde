imports: {}
exports:
  - IsEmpty
parts:
  IsEmpty:
    id: IsEmpty
    inputs:
      str:
        mode: required
        type: any
    outputs:
      "true":
        type: any
      "false":
        type: any
    fnCode: |-
      const { str } = inputs;
              
      // magic here
      if (str === "") {
          outputs.true.next(str);
      } else {
          outputs.false.next(str);
      }
exposedFunctionality: []
