imports: {}
exports:
  - epochToString
parts:
  epochToString:
    id: epochToString
    inputs:
      epoch:
        mode: required
        type: any
    outputs:
      r:
        type: any
    completionOutputs:
      - r
    fnCode: |-
      // magic here

      const d = new Date(inputs.epoch);
      outputs.r.next(d.toLocaleString());
exposedFunctionality: []
