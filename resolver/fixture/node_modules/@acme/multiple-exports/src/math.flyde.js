const add = {
    id: "Add",
    inputs: {"a": {"mode": "required"}},
    outputs: {"r": {}},
    run: (inputs, outputs) => {
        outputs.r.next(inputs.a + 1);
    }
}

const sub = {
    id: "Sub",
    inputs: {"a": {"mode": "required"}},
    outputs: {"r": {}},
    run: (inputs, outputs) => {
        outputs.r.next(inputs.a - 1);
    }
}
module.exports = {
    add,
    sub
}