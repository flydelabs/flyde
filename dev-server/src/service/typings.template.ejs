/// <reference path="<%-MODULE_NAME%>/index.d.ts" />

/*
    This file is generated by flyde-cli. Please do not edit it.
*/

declare module '<%-MODULE_NAME%>' {

    // generated flow typings
    <%_ locatedFlows.forEach(function(locatedFlow){ _%>
        <%_ if (locatedFlow.flow === 'corrupt' || !locatedFlow.flow.mainId) { _%>
    /**
    * @deprecated Path "<%- locatedFlow.relativePath %>"" is corrupt. Please fix it. <%_ if (locatedFlow.flow !== 'corrupt') { _%>"mainId" is missing. <%_ } _%>
    */
    export function executeFlow(path: '<%-locatedFlow.fileName%>', args: never): never;
        <%_} else {_%>
    export function executeFlow(path: '<%-locatedFlow.fileName%>', args: {<%- Object.keys(locatedFlow.flow.parts[locatedFlow.flow.mainId].inputs || {}).map(k => `${k}: any`).join(', ') %>}): Promise<any>;
        <%_} _%>
    <%_ }); _%>   

    // repeating static function typings.. TODO - find a way to avoid needing this
    export const expose: (func: Function, name: string, inputs?: string[]) => void;
}
